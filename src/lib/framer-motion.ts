import type { UseInViewOptions, Variants } from "framer-motion";
import { FADE_IN_VARIANTS, FRAMER_MOTION_OPTIONS } from "../constants";

// Re-export animation constants for convenience
export const fadeInVariants: Variants = FADE_IN_VARIANTS;
export const framerMotionOptions: UseInViewOptions = FRAMER_MOTION_OPTIONS;
